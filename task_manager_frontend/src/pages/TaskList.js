import React from 'react';

// PUBLIC_INTERFACE
function TaskList() {
  /** TaskList page that lists all tasks for the user. */
  return (
    <div className="container">
      <h2>Task List</h2>
      <p>This is the task list page. (UI not implemented yet.)</p>
    </div>
  );
}

export default TaskList;
